{
  "schema_version": 1,
  "slug": "in1d",
  "detail_href": "/docs/api/ops/in1d/",
  "detail_json_href": "/api-data/ops/in1d.json",
  "source": {
    "whest": "https://github.com/AIcrowd/whest/blob/main/src/whest/_ndarray.py#L338-L348",
    "numpy": "https://numpy.org/doc/stable/reference/generated/numpy.in1d.html"
  },
  "op": {
    "name": "in1d",
    "module": "numpy",
    "whest_ref": "we.in1d",
    "numpy_ref": "np.in1d",
    "category": "counted_custom",
    "status": "supported",
    "weight": 1.0,
    "cost_formula": "per-operation",
    "cost_formula_latex": "varies",
    "notes": "Set membership; cost = (n+m)*ceil(log2(n+m)).",
    "summary": "Test whether each element of a 1-D array is also present in a second array.",
    "signature": "we.in1d(ar1, ar2, assume_unique=False, invert=False, *, kind=None)"
  },
  "docs": {
    "sections": [
      {
        "kind": "summary",
        "title": "Summary",
        "blocks": [
          {
            "type": "paragraph",
            "text": "Test whether each element of a 1-D array is also present in a second array."
          },
          {
            "type": "directive",
            "name": "deprecated:: 2.0",
            "body": "    Use :func:`isin` instead of `in1d` for new code.",
            "raw": ".. deprecated:: 2.0\n    Use :func:`isin` instead of `in1d` for new code."
          },
          {
            "type": "paragraph",
            "text": "Returns a boolean array the same length as `ar1` that is True\nwhere an element of `ar1` is in `ar2` and False otherwise."
          }
        ]
      },
      {
        "kind": "parameters",
        "title": "Parameters",
        "items": [
          {
            "name": "ar1",
            "type": "(M,) array_like",
            "desc_blocks": [
              {
                "type": "paragraph",
                "text": "Input array."
              }
            ]
          },
          {
            "name": "ar2",
            "type": "array_like",
            "desc_blocks": [
              {
                "type": "paragraph",
                "text": "The values against which to test each value of `ar1`."
              }
            ]
          },
          {
            "name": "assume_unique",
            "type": "bool, optional",
            "desc_blocks": [
              {
                "type": "paragraph",
                "text": "If True, the input arrays are both assumed to be unique, which\ncan speed up the calculation.  Default is False."
              }
            ]
          },
          {
            "name": "invert",
            "type": "bool, optional",
            "desc_blocks": [
              {
                "type": "paragraph",
                "text": "If True, the values in the returned array are inverted (that is,\nFalse where an element of `ar1` is in `ar2` and True otherwise).\nDefault is False. ``np.in1d(a, b, invert=True)`` is equivalent\nto (but is faster than) ``np.invert(in1d(a, b))``."
              }
            ]
          },
          {
            "name": "kind",
            "type": "{None, 'sort', 'table'}, optional",
            "desc_blocks": [
              {
                "type": "paragraph",
                "text": "The algorithm to use. This will not affect the final result,\nbut will affect the speed and memory use. The default, None,\nwill select automatically based on memory considerations."
              },
              {
                "type": "paragraph",
                "text": "* If 'sort', will use a mergesort-based approach. This will have\n  a memory usage of roughly 6 times the sum of the sizes of\n  `ar1` and `ar2`, not accounting for size of dtypes.\n* If 'table', will use a lookup table approach similar\n  to a counting sort. This is only available for boolean and\n  integer arrays. This will have a memory usage of the\n  size of `ar1` plus the max-min value of `ar2`. `assume_unique`\n  has no effect when the 'table' option is used.\n* If None, will automatically choose 'table' if\n  the required memory allocation is less than or equal to\n  6 times the sum of the sizes of `ar1` and `ar2`,\n  otherwise will use 'sort'. This is done to not use\n  a large amount of memory by default, even though\n  'table' may be faster in most cases. If 'table' is chosen,\n  `assume_unique` will have no effect."
              }
            ]
          }
        ]
      },
      {
        "kind": "returns",
        "title": "Returns",
        "items": [
          {
            "name": "in1d",
            "type": "(M,) ndarray, bool",
            "desc_blocks": [
              {
                "type": "paragraph",
                "text": "The values `ar1[in1d]` are in `ar2`."
              }
            ]
          }
        ]
      },
      {
        "kind": "notes",
        "title": "Notes",
        "blocks": [
          {
            "type": "paragraph",
            "text": "`in1d` can be considered as an element-wise function version of the\npython keyword `in`, for 1-D sequences. ``in1d(a, b)`` is roughly\nequivalent to ``np.array([item in b for item in a])``.\nHowever, this idea fails if `ar2` is a set, or similar (non-sequence)\ncontainer:  As ``ar2`` is converted to an array, in those cases\n``asarray(ar2)`` is an object array rather than the expected array of\ncontained values."
          },
          {
            "type": "paragraph",
            "text": "Using ``kind='table'`` tends to be faster than `kind='sort'` if the\nfollowing relationship is true:\n``log10(len(ar2)) > (log10(max(ar2)-min(ar2)) - 2.27) / 0.927``,\nbut may use greater memory. The default value for `kind` will\nbe automatically selected based only on memory usage, so one may\nmanually set ``kind='table'`` if memory constraints can be relaxed."
          }
        ]
      },
      {
        "kind": "examples",
        "title": "Examples",
        "blocks": [
          {
            "type": "code",
            "language": "text",
            "code": ">>> import numpy as np\n>>> test = np.array([0, 1, 2, 5, 0])\n>>> states = [0, 2]\n>>> mask = np.in1d(test, states)\n>>> mask\narray([ True, False,  True, False,  True])\n>>> test[mask]\narray([0, 2, 0])\n>>> mask = np.in1d(test, states, invert=True)\n>>> mask\narray([False,  True, False,  True, False])\n>>> test[mask]\narray([1, 5])"
          }
        ]
      },
      {
        "kind": "see-also",
        "title": "See Also",
        "blocks": [
          {
            "type": "paragraph",
            "text": "isin \u2014 Version of this function that preserves the shape of ar1."
          }
        ]
      }
    ]
  }
}