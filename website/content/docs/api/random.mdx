---
title: random
description: Counted samplers, generator helpers, and random-specific public utilities under `we.random`.
---

`we.random` groups the public random surface: direct namespace exports at the
top, then canonical generated leaves for distribution samplers and RNG state
helpers.

<NamespaceSymbolList namespace="random" />
