---
title: stats
description: Distribution objects and canonical method pages under `we.stats`.
---

`we.stats` exposes the authored distribution objects first. Use each object's
linked child methods to jump into the generated `cdf`, `pdf`, and `ppf` leaves.

<NamespaceSymbolList namespace="stats" />
