---
title: "Operation Audit"
---

{/* Auto-generated by scripts/generate_api_docs.py. Do not edit manually. */}

Complete inventory of every NumPy operation and its whest status.
Generated from the operation registry (`_registry.py`).

## Summary

| Category | Count | Cost |
|----------|-------|------|
| free | 80 | 0 FLOPs |
| counted_unary | 71 | $\text\{numel\}(\text\{output\})$ |
| counted_binary | 47 | $\text\{numel\}(\text\{output\})$ |
| counted_reduction | 37 | $\text\{numel\}(\text\{input\})$ |
| counted_custom | 238 | Per-operation formula |
| blacklisted | 35 | Not available |
| **Total** | **508** | |

## All Operations

| Operation | whest | NumPy | Category | Cost | Status | Notes |
|-----------|-----------|-------|----------|------|--------|-------|
| `abs` | `we.abs` | `np.abs` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise absolute value; alias for absolute. |
| `absolute` | `we.absolute` | `np.absolute` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise absolute value. |
| `acos` | `we.acos` | `np.acos` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Alias for arccos (NumPy 2.x). |
| `acosh` | `we.acosh` | `np.acosh` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Alias for arccosh (NumPy 2.x). |
| `add` | `we.add` | `np.add` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise addition. |
| `all` | `we.all` | `np.all` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Test whether all array elements are true. |
| `allclose` | `we.allclose` | `np.allclose` | counted_custom | varies | 🟠 supported | Element-wise tolerance check; cost = numel(a). |
| `amax` | `we.amax` | `np.amax` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Maximum value of array (alias for max/numpy.amax). |
| `amin` | `we.amin` | `np.amin` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Minimum value of array (alias for min/numpy.amin). |
| `angle` | `we.angle` | `np.angle` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Return angle of complex argument element-wise. |
| `any` | `we.any` | `np.any` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Test whether any array element is true. |
| `append` | `we.append` | `np.append` | counted_custom | varies | 🟠 supported | Append values to end of array. Cost: numel(values). |
| `apply_along_axis` | `we.apply_along_axis` | `np.apply_along_axis` | counted_custom | varies | 🟠 supported | Apply function along axis. Cost: numel(output). Inner function costs tracked separately. |
| `apply_over_axes` | `we.apply_over_axes` | `np.apply_over_axes` | counted_custom | varies | 🟠 supported | Apply function over multiple axes. Cost: numel(output). |
| `arange` | `we.arange` | `np.arange` | counted_custom | varies | 🟠 supported | Return evenly spaced values in given interval. Cost: numel(output). |
| `arccos` | `we.arccos` | `np.arccos` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise inverse cosine. |
| `arccosh` | `we.arccosh` | `np.arccosh` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise inverse hyperbolic cosine. |
| `arcsin` | `we.arcsin` | `np.arcsin` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise inverse sine. |
| `arcsinh` | `we.arcsinh` | `np.arcsinh` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise inverse hyperbolic sine. |
| `arctan` | `we.arctan` | `np.arctan` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise inverse tangent. |
| `arctan2` | `we.arctan2` | `np.arctan2` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise arctan(y/x) considering quadrant. |
| `arctanh` | `we.arctanh` | `np.arctanh` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise inverse hyperbolic tangent. |
| `argmax` | `we.argmax` | `np.argmax` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Index of maximum value. |
| `argmin` | `we.argmin` | `np.argmin` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Index of minimum value. |
| `argpartition` | `we.argpartition` | `np.argpartition` | counted_custom | varies | 🟠 supported | Indirect partition; cost = n per slice. |
| `argsort` | `we.argsort` | `np.argsort` | counted_custom | varies | 🟠 supported | Indirect sort; cost = n*ceil(log2(n)) per slice. |
| `argwhere` | `we.argwhere` | `np.argwhere` | counted_custom | varies | 🟠 supported | Find indices of non-zero elements. Cost: numel(input). |
| `around` | `we.around` | `np.around` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Alias for round. |
| `array` | `we.array` | `np.array` | counted_custom | varies | 🟠 supported | Create array from data. Cost: numel(input). |
| `array2string` | — | `np.array2string` | blacklisted | N/A | 🔴 blocked | Return string representation of array. Not supported. |
| `array_equal` | `we.array_equal` | `np.array_equal` | counted_custom | varies | 🟠 supported | Element-wise equality; cost = numel(a). |
| `array_equiv` | `we.array_equiv` | `np.array_equiv` | counted_custom | varies | 🟠 supported | Element-wise equivalence; cost = numel(a). |
| `array_repr` | — | `np.array_repr` | blacklisted | N/A | 🔴 blocked | Return string representation of array. Not supported. |
| `array_split` | `we.array_split` | `np.array_split` | free | $0$ | 🟢 supported | Split array into sub-arrays (possibly unequal). Cost: numel(output). |
| `array_str` | — | `np.array_str` | blacklisted | N/A | 🔴 blocked | Return string representation of data in array. Not supported. |
| `asarray` | `we.asarray` | `np.asarray` | free | $0$ | 🟢 supported | Convert input to array. Cost: numel(input). |
| `asarray_chkfinite` | `we.asarray_chkfinite` | `np.asarray_chkfinite` | counted_custom | varies | 🟠 supported | Convert to array, raising if NaN or inf. Cost: numel(input). |
| `asin` | `we.asin` | `np.asin` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Alias for arcsin (NumPy 2.x). |
| `asinh` | `we.asinh` | `np.asinh` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Alias for arcsinh (NumPy 2.x). |
| `asmatrix` | — | `np.asmatrix` | blacklisted | N/A | 🔴 blocked | Interpret input as matrix (deprecated). Not supported. |
| `astype` | `we.astype` | `np.astype` | free | $0$ | 🟢 supported | Cast array to specified type. |
| `atan` | `we.atan` | `np.atan` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Alias for arctan (NumPy 2.x). |
| `atan2` | `we.atan2` | `np.atan2` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Alias for arctan2 (NumPy 2.x). |
| `atanh` | `we.atanh` | `np.atanh` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Alias for arctanh (NumPy 2.x). |
| `atleast_1d` | `we.atleast_1d` | `np.atleast_1d` | free | $0$ | 🟢 supported | View inputs as arrays with at least one dimension. |
| `atleast_2d` | `we.atleast_2d` | `np.atleast_2d` | free | $0$ | 🟢 supported | View inputs as arrays with at least two dimensions. |
| `atleast_3d` | `we.atleast_3d` | `np.atleast_3d` | free | $0$ | 🟢 supported | View inputs as arrays with at least three dimensions. |
| `average` | `we.average` | `np.average` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Weighted average of array elements. |
| `bartlett` | `we.bartlett` | `np.bartlett` | counted_custom | $n$ | 🟠 supported | Bartlett window. Cost: n (one linear eval per sample). |
| `base_repr` | — | `np.base_repr` | blacklisted | N/A | 🔴 blocked | Return string representation of number in given base. Cost: numel(input). |
| `binary_repr` | — | `np.binary_repr` | blacklisted | N/A | 🔴 blocked | Return binary string representation of the input number. Cost: numel(input). |
| `bincount` | `we.bincount` | `np.bincount` | counted_custom | varies | 🟠 supported | Integer counting; cost = numel(x). |
| `bitwise_and` | `we.bitwise_and` | `np.bitwise_and` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise bitwise AND. |
| `bitwise_count` | `we.bitwise_count` | `np.bitwise_count` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Count set bits element-wise (popcount). |
| `bitwise_invert` | `we.bitwise_invert` | `np.bitwise_invert` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise bitwise invert (alias for bitwise_not). |
| `bitwise_left_shift` | `we.bitwise_left_shift` | `np.bitwise_left_shift` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise left bit shift. |
| `bitwise_not` | `we.bitwise_not` | `np.bitwise_not` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise bitwise NOT. |
| `bitwise_or` | `we.bitwise_or` | `np.bitwise_or` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise bitwise OR. |
| `bitwise_right_shift` | `we.bitwise_right_shift` | `np.bitwise_right_shift` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise right bit shift. |
| `bitwise_xor` | `we.bitwise_xor` | `np.bitwise_xor` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise bitwise XOR. |
| `blackman` | `we.blackman` | `np.blackman` | counted_custom | $3n$ | 🟠 supported | Blackman window. Cost: 3*n (three cosine terms per sample). |
| `block` | `we.block` | `np.block` | counted_custom | varies | 🟠 supported | Assemble ndarray from nested list of blocks. Cost: numel(output). |
| `bmat` | `we.bmat` | `np.bmat` | counted_custom | varies | 🟠 supported | Build matrix from nested list of matrices. Cost: numel(output). |
| `broadcast_arrays` | `we.broadcast_arrays` | `np.broadcast_arrays` | free | $0$ | 🟢 supported | Broadcast arrays against each other. Cost: numel(output). |
| `broadcast_shapes` | `we.broadcast_shapes` | `np.broadcast_shapes` | free | $0$ | 🟢 supported | Compute broadcast shape from input shapes. |
| `broadcast_to` | `we.broadcast_to` | `np.broadcast_to` | free | $0$ | 🟢 supported | Broadcast array to new shape. Cost: numel(output). |
| `busday_count` | — | `np.busday_count` | blacklisted | N/A | 🔴 blocked | Count valid days between begindate and enddate. Not supported. |
| `busday_offset` | — | `np.busday_offset` | blacklisted | N/A | 🔴 blocked | Apply offset to dates subject to valid day rules. Not supported. |
| `can_cast` | `we.can_cast` | `np.can_cast` | free | $0$ | 🟢 supported | Returns True if cast is safe. |
| `cbrt` | `we.cbrt` | `np.cbrt` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise cube root. |
| `ceil` | `we.ceil` | `np.ceil` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise ceiling. |
| `choose` | `we.choose` | `np.choose` | counted_custom | varies | 🟠 supported | Construct array from index array and choices. Cost: numel(output). |
| `clip` | `we.clip` | `np.clip` | counted_custom | $\text\{numel\}(\text\{input\})$ | 🟠 supported | Clip array to [a_min, a_max] element-wise. |
| `column_stack` | `we.column_stack` | `np.column_stack` | free | $0$ | 🟢 supported | Stack 1-D arrays as columns into 2-D array. |
| `common_type` | `we.common_type` | `np.common_type` | free | $0$ | 🟢 supported | Return scalar type common to all input arrays. |
| `compress` | `we.compress` | `np.compress` | counted_custom | varies | 🟠 supported | Return selected slices along axis. Cost: numel(input). |
| `concat` | `we.concat` | `np.concat` | counted_custom | varies | 🟠 supported | Join arrays along axis (NumPy 2.x array API alias for concatenate). Cost: numel(output). |
| `concatenate` | `we.concatenate` | `np.concatenate` | counted_custom | varies | 🟠 supported | Join arrays along axis. Cost: numel(output). |
| `conj` | `we.conj` | `np.conj` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Complex conjugate element-wise. |
| `conjugate` | `we.conjugate` | `np.conjugate` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Complex conjugate element-wise. |
| `convolve` | `we.convolve` | `np.convolve` | counted_custom | $n \cdot m$ | 🟠 supported | 1-D discrete convolution. |
| `copy` | `we.copy` | `np.copy` | free | $0$ | 🟢 supported | Return array copy. |
| `copysign` | `we.copysign` | `np.copysign` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Copy sign of x2 to magnitude of x1 element-wise. |
| `copyto` | `we.copyto` | `np.copyto` | counted_custom | varies | 🟠 supported | Copy values from src to dst array. Cost: num copied. |
| `corrcoef` | `we.corrcoef` | `np.corrcoef` | counted_custom | $n^2 \cdot m$ | 🟠 supported | Pearson correlation coefficients. |
| `correlate` | `we.correlate` | `np.correlate` | counted_custom | $n \cdot m$ | 🟠 supported | 1-D cross-correlation. |
| `cos` | `we.cos` | `np.cos` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise cosine. |
| `cosh` | `we.cosh` | `np.cosh` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise hyperbolic cosine. |
| `count_nonzero` | `we.count_nonzero` | `np.count_nonzero` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Count non-zero elements. |
| `cov` | `we.cov` | `np.cov` | counted_custom | $n^2 \cdot m$ | 🟠 supported | Covariance matrix. |
| `cross` | `we.cross` | `np.cross` | counted_custom | $\text\{numel\}(\text\{output\})$ | 🟠 supported | Cross product of two 3-D vectors. |
| `cumprod` | `we.cumprod` | `np.cumprod` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Cumulative product of array elements. |
| `cumsum` | `we.cumsum` | `np.cumsum` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Cumulative sum of array elements. |
| `cumulative_prod` | `we.cumulative_prod` | `np.cumulative_prod` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Cumulative product (NumPy 2.x array API). |
| `cumulative_sum` | `we.cumulative_sum` | `np.cumulative_sum` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Cumulative sum (NumPy 2.x array API). |
| `datetime_as_string` | — | `np.datetime_as_string` | blacklisted | N/A | 🔴 blocked | Convert datetime array to string representation. Not supported. |
| `datetime_data` | — | `np.datetime_data` | blacklisted | N/A | 🔴 blocked | Get information about the step size of datetime dtype. Not supported. |
| `deg2rad` | `we.deg2rad` | `np.deg2rad` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Alias for radians. |
| `degrees` | `we.degrees` | `np.degrees` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Convert radians to degrees element-wise. |
| `delete` | `we.delete` | `np.delete` | counted_custom | varies | 🟠 supported | Return array with sub-arrays deleted along axis. Cost: num deleted. |
| `diag` | `we.diag` | `np.diag` | counted_custom | varies | 🟠 supported | Extract diagonal or construct diagonal array. Cost: len(diagonal). |
| `diag_indices` | `we.diag_indices` | `np.diag_indices` | free | $0$ | 🟢 supported | Return indices to access main diagonal of n-D array. |
| `diag_indices_from` | `we.diag_indices_from` | `np.diag_indices_from` | free | $0$ | 🟢 supported | Return indices to access main diagonal of given array. |
| `diagflat` | `we.diagflat` | `np.diagflat` | counted_custom | varies | 🟠 supported | Create diagonal array from flattened input. Cost: len(v). |
| `diagonal` | `we.diagonal` | `np.diagonal` | counted_custom | varies | 🟠 supported | Return specified diagonals. Cost: numel(input). |
| `diff` | `we.diff` | `np.diff` | counted_custom | $\text\{numel\}(\text\{input\})$ | 🟠 supported | n-th discrete difference along axis. |
| `digitize` | `we.digitize` | `np.digitize` | counted_custom | varies | 🟠 supported | Bin search; cost = n*ceil(log2(bins)). |
| `divide` | `we.divide` | `np.divide` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise true division. |
| `divmod` | `we.divmod` | `np.divmod` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise (quotient, remainder) tuple. |
| `dot` | `we.dot` | `np.dot` | counted_custom | $m \cdot k \cdot n$ | 🟠 supported | Dot product; cost = M*K*N (FMA=1). |
| `dsplit` | `we.dsplit` | `np.dsplit` | free | $0$ | 🟢 supported | Split array into multiple sub-arrays depth-wise. Cost: numel(output). |
| `dstack` | `we.dstack` | `np.dstack` | counted_custom | varies | 🟠 supported | Stack arrays depth-wise (along third axis). Cost: numel(output). |
| `ediff1d` | `we.ediff1d` | `np.ediff1d` | counted_custom | $\text\{numel\}(\text\{input\})$ | 🟠 supported | Differences between consecutive elements. |
| `einsum` | `we.einsum` | `np.einsum` | counted_custom | $\text\{op\_factor\} \cdot \prod_i d_i$ | 🟠 supported | Generalized Einstein summation. |
| `einsum_path` | `we.einsum_path` | `np.einsum_path` | counted_custom | $0$ | 🟠 supported | Optimize einsum contraction path (no numeric output). |
| `empty` | `we.empty` | `np.empty` | free | $0$ | 🟢 supported | Uninitialized array allocation. |
| `empty_like` | `we.empty_like` | `np.empty_like` | free | $0$ | 🟢 supported | Uninitialized array with same shape/type as input. |
| `equal` | `we.equal` | `np.equal` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise x1 == x2. |
| `exp` | `we.exp` | `np.exp` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise e^x. |
| `exp2` | `we.exp2` | `np.exp2` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise 2^x. |
| `expand_dims` | `we.expand_dims` | `np.expand_dims` | free | $0$ | 🟢 supported | Insert new size-1 axis. |
| `expm1` | `we.expm1` | `np.expm1` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise e^x - 1 (accurate near zero). |
| `extract` | `we.extract` | `np.extract` | counted_custom | varies | 🟠 supported | Return elements satisfying condition. Cost: numel(input). |
| `eye` | `we.eye` | `np.eye` | free | $0$ | 🟢 supported | Create identity matrix. |
| `fabs` | `we.fabs` | `np.fabs` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise absolute value (always float). |
| `fft.fft` | `we.fft.fft` | `np.fft.fft` | counted_custom | $5n \cdot \lceil\log_2 n\rceil$ | 🟠 supported | 1-D complex FFT. Cost: 5*n*ceil(log2(n)) (Cooley-Tukey radix-2; Van Loan 1992 §1.4). |
| `fft.fft2` | `we.fft.fft2` | `np.fft.fft2` | counted_custom | $5N \cdot \lceil\log_2 N\rceil$ | 🟠 supported | 2-D complex FFT. Cost: 5*N*ceil(log2(N)), N=prod(s) (Cooley-Tukey radix-2; Van Loan 1992 §1.4). |
| `fft.fftfreq` | `we.fft.fftfreq` | `np.fft.fftfreq` | free | $0$ | 🟢 supported | FFT sample frequencies. No arithmetic; returns index array. |
| `fft.fftn` | `we.fft.fftn` | `np.fft.fftn` | counted_custom | $5N \cdot \lceil\log_2 N\rceil$ | 🟠 supported | N-D complex FFT. Cost: 5*N*ceil(log2(N)), N=prod(s) (Cooley-Tukey radix-2; Van Loan 1992 §1.4). |
| `fft.fftshift` | `we.fft.fftshift` | `np.fft.fftshift` | free | $0$ | 🟢 supported | Shift zero-frequency component to center. No arithmetic; index reordering only. |
| `fft.hfft` | `we.fft.hfft` | `np.fft.hfft` | counted_custom | $5n \cdot \lceil\log_2 n\rceil$ | 🟠 supported | FFT of Hermitian-symmetric signal. Cost: 5*n_out*ceil(log2(n_out)) (Cooley-Tukey radix-2; Van Loan 1992 §1.4). |
| `fft.ifft` | `we.fft.ifft` | `np.fft.ifft` | counted_custom | $5n \cdot \lceil\log_2 n\rceil$ | 🟠 supported | Inverse 1-D complex FFT. Cost: 5*n*ceil(log2(n)) (Cooley-Tukey radix-2; Van Loan 1992 §1.4). |
| `fft.ifft2` | `we.fft.ifft2` | `np.fft.ifft2` | counted_custom | $5N \cdot \lceil\log_2 N\rceil$ | 🟠 supported | Inverse 2-D complex FFT. Cost: 5*N*ceil(log2(N)), N=prod(s) (Cooley-Tukey radix-2; Van Loan 1992 §1.4). |
| `fft.ifftn` | `we.fft.ifftn` | `np.fft.ifftn` | counted_custom | $5N \cdot \lceil\log_2 N\rceil$ | 🟠 supported | Inverse N-D complex FFT. Cost: 5*N*ceil(log2(N)), N=prod(s) (Cooley-Tukey radix-2; Van Loan 1992 §1.4). |
| `fft.ifftshift` | `we.fft.ifftshift` | `np.fft.ifftshift` | free | $0$ | 🟢 supported | Inverse of fftshift. No arithmetic; index reordering only. |
| `fft.ihfft` | `we.fft.ihfft` | `np.fft.ihfft` | counted_custom | $5n \cdot \lceil\log_2 n\rceil$ | 🟠 supported | Inverse FFT of Hermitian signal. Cost: 5*n*ceil(log2(n)) (Cooley-Tukey radix-2; Van Loan 1992 §1.4). |
| `fft.irfft` | `we.fft.irfft` | `np.fft.irfft` | counted_custom | $5(n/2) \cdot \lceil\log_2 n\rceil$ | 🟠 supported | Inverse 1-D real FFT. Cost: 5*(n//2)*ceil(log2(n)) (Cooley-Tukey radix-2; Van Loan 1992 §1.4). |
| `fft.irfft2` | `we.fft.irfft2` | `np.fft.irfft2` | counted_custom | $5(N/2) \cdot \lceil\log_2 N\rceil$ | 🟠 supported | Inverse 2-D real FFT. Cost: 5*(N//2)*ceil(log2(N)), N=prod(s) (Cooley-Tukey radix-2; Van Loan 1992 §1.4). |
| `fft.irfftn` | `we.fft.irfftn` | `np.fft.irfftn` | counted_custom | $5(N/2) \cdot \lceil\log_2 N\rceil$ | 🟠 supported | Inverse N-D real FFT. Cost: 5*(N//2)*ceil(log2(N)), N=prod(s) (Cooley-Tukey radix-2; Van Loan 1992 §1.4). |
| `fft.rfft` | `we.fft.rfft` | `np.fft.rfft` | counted_custom | $5(n/2) \cdot \lceil\log_2 n\rceil$ | 🟠 supported | 1-D real FFT. Cost: 5*(n//2)*ceil(log2(n)) (Cooley-Tukey radix-2; Van Loan 1992 §1.4). |
| `fft.rfft2` | `we.fft.rfft2` | `np.fft.rfft2` | counted_custom | $5(N/2) \cdot \lceil\log_2 N\rceil$ | 🟠 supported | 2-D real FFT. Cost: 5*(N//2)*ceil(log2(N)), N=prod(s) (Cooley-Tukey radix-2; Van Loan 1992 §1.4). |
| `fft.rfftfreq` | `we.fft.rfftfreq` | `np.fft.rfftfreq` | free | $0$ | 🟢 supported | Real FFT sample frequencies. No arithmetic; returns index array. |
| `fft.rfftn` | `we.fft.rfftn` | `np.fft.rfftn` | counted_custom | $5(N/2) \cdot \lceil\log_2 N\rceil$ | 🟠 supported | N-D real FFT. Cost: 5*(N//2)*ceil(log2(N)), N=prod(s) (Cooley-Tukey radix-2; Van Loan 1992 §1.4). |
| `fill_diagonal` | `we.fill_diagonal` | `np.fill_diagonal` | counted_custom | varies | 🟠 supported | Fill main diagonal of given array. Cost: min(m,n). |
| `fix` | `we.fix` | `np.fix` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Round toward zero element-wise (alias for trunc). |
| `flatnonzero` | `we.flatnonzero` | `np.flatnonzero` | counted_custom | varies | 🟠 supported | Return indices of non-zero elements in flattened array. Cost: numel(input). |
| `flip` | `we.flip` | `np.flip` | free | $0$ | 🟢 supported | Reverse order of elements along axis. |
| `fliplr` | `we.fliplr` | `np.fliplr` | free | $0$ | 🟢 supported | Flip array left-right. |
| `flipud` | `we.flipud` | `np.flipud` | free | $0$ | 🟢 supported | Flip array up-down. |
| `float_power` | `we.float_power` | `np.float_power` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise exponentiation in float64. |
| `floor` | `we.floor` | `np.floor` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise floor. |
| `floor_divide` | `we.floor_divide` | `np.floor_divide` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise floor division. |
| `fmax` | `we.fmax` | `np.fmax` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise maximum ignoring NaN. |
| `fmin` | `we.fmin` | `np.fmin` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise minimum ignoring NaN. |
| `fmod` | `we.fmod` | `np.fmod` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise C-style fmod (remainder toward zero). |
| `format_float_positional` | — | `np.format_float_positional` | blacklisted | N/A | 🔴 blocked | Format floating point scalar as decimal string. Not supported. |
| `format_float_scientific` | — | `np.format_float_scientific` | blacklisted | N/A | 🔴 blocked | Format floating point scalar as scientific notation. Not supported. |
| `frexp` | `we.frexp` | `np.frexp` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Decompose x into mantissa and exponent element-wise. |
| `from_dlpack` | `we.from_dlpack` | `np.from_dlpack` | free | $0$ | 🟢 supported | Create ndarray from DLPack object (zero-copy). Cost: numel(output). |
| `frombuffer` | `we.frombuffer` | `np.frombuffer` | free | $0$ | 🟢 supported | Interpret buffer as 1-D array. Cost: numel(output). |
| `fromfile` | — | `np.fromfile` | blacklisted | N/A | 🔴 blocked | Construct array from binary/text file. Cost: numel(output). |
| `fromfunction` | `we.fromfunction` | `np.fromfunction` | counted_custom | varies | 🟠 supported | Construct array by executing function over each coordinate. Cost: numel(output). |
| `fromiter` | `we.fromiter` | `np.fromiter` | counted_custom | varies | 🟠 supported | Create array from an iterable. Cost: numel(output). |
| `frompyfunc` | — | `np.frompyfunc` | blacklisted | N/A | 🔴 blocked | Take arbitrary Python function and return NumPy ufunc. Not supported. |
| `fromregex` | — | `np.fromregex` | blacklisted | N/A | 🔴 blocked | Construct array from text file using regex. Cost: numel(output). |
| `fromstring` | — | `np.fromstring` | blacklisted | N/A | 🔴 blocked | Create 1-D array from string data. Cost: numel(output). |
| `full` | `we.full` | `np.full` | counted_custom | varies | 🟠 supported | Create array filled with scalar value. Cost: num copied. |
| `full_like` | `we.full_like` | `np.full_like` | counted_custom | varies | 🟠 supported | Array filled with scalar, same shape/type as input. Cost: numel(output). |
| `gcd` | `we.gcd` | `np.gcd` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise greatest common divisor. |
| `genfromtxt` | — | `np.genfromtxt` | blacklisted | N/A | 🔴 blocked | Load data from text file with missing values. Not supported. |
| `geomspace` | `we.geomspace` | `np.geomspace` | counted_custom | varies | 🟠 supported | Geometric-spaced generation; cost = num. |
| `get_include` | — | `np.get_include` | blacklisted | N/A | 🔴 blocked | Return directory containing NumPy C header files. Not supported. |
| `getbufsize` | — | `np.getbufsize` | blacklisted | N/A | 🔴 blocked | Return size of buffer used in ufuncs. Not supported. |
| `geterr` | — | `np.geterr` | blacklisted | N/A | 🔴 blocked | Get current way of handling floating-point errors. Not supported. |
| `geterrcall` | — | `np.geterrcall` | blacklisted | N/A | 🔴 blocked | Return current callback function for floating-point errors. Not supported. |
| `gradient` | `we.gradient` | `np.gradient` | counted_custom | $\text\{numel\}(\text\{input\})$ | 🟠 supported | Gradient using central differences. |
| `greater` | `we.greater` | `np.greater` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise x1 &gt; x2. |
| `greater_equal` | `we.greater_equal` | `np.greater_equal` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise x1 &ge; x2. |
| `hamming` | `we.hamming` | `np.hamming` | counted_custom | $n$ | 🟠 supported | Hamming window. Cost: n (one cosine per sample). |
| `hanning` | `we.hanning` | `np.hanning` | counted_custom | $n$ | 🟠 supported | Hanning window. Cost: n (one cosine per sample). |
| `heaviside` | `we.heaviside` | `np.heaviside` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Heaviside step function element-wise. |
| `histogram` | `we.histogram` | `np.histogram` | counted_custom | varies | 🟠 supported | Binning; cost = n*ceil(log2(bins)). |
| `histogram2d` | `we.histogram2d` | `np.histogram2d` | counted_custom | varies | 🟠 supported | 2D binning; cost = n*(ceil(log2(bx))+ceil(log2(by))). |
| `histogram_bin_edges` | `we.histogram_bin_edges` | `np.histogram_bin_edges` | counted_custom | varies | 🟠 supported | Bin edge computation; cost = numel(a). |
| `histogramdd` | `we.histogramdd` | `np.histogramdd` | counted_custom | varies | 🟠 supported | ND binning; cost = n*sum(ceil(log2(b_i))). |
| `hsplit` | `we.hsplit` | `np.hsplit` | free | $0$ | 🟢 supported | Split array into columns. |
| `hstack` | `we.hstack` | `np.hstack` | free | $0$ | 🟢 supported | Stack arrays horizontally. |
| `hypot` | `we.hypot` | `np.hypot` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise Euclidean norm sqrt(x1^2 + x2^2). |
| `i0` | `we.i0` | `np.i0` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Modified Bessel function of order 0, element-wise. |
| `identity` | `we.identity` | `np.identity` | free | $0$ | 🟢 supported | Create square identity matrix. |
| `imag` | `we.imag` | `np.imag` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Return imaginary part of complex array. |
| `in1d` | `we.in1d` | `np.in1d` | counted_custom | varies | 🟠 supported | Set membership; cost = (n+m)*ceil(log2(n+m)). |
| `indices` | `we.indices` | `np.indices` | counted_custom | varies | 🟠 supported | Return array representing indices of a grid. Cost: numel(output). |
| `inner` | `we.inner` | `np.inner` | counted_custom | $n$ | 🟠 supported | Inner product; cost = N (FMA=1). |
| `insert` | `we.insert` | `np.insert` | counted_custom | varies | 🟠 supported | Insert values along axis before given indices. Cost: numel(values). |
| `interp` | `we.interp` | `np.interp` | counted_custom | $n \cdot \log m$ | 🟠 supported | 1-D linear interpolation. |
| `intersect1d` | `we.intersect1d` | `np.intersect1d` | counted_custom | varies | 🟠 supported | Set intersection; cost = (n+m)*ceil(log2(n+m)). |
| `invert` | `we.invert` | `np.invert` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Bitwise NOT element-wise. |
| `is_busday` | — | `np.is_busday` | blacklisted | N/A | 🔴 blocked | Calculates which of given dates are valid days. Not supported. |
| `isclose` | `we.isclose` | `np.isclose` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise approximate equality test. |
| `iscomplex` | `we.iscomplex` | `np.iscomplex` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Test if element is complex element-wise. |
| `iscomplexobj` | `we.iscomplexobj` | `np.iscomplexobj` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Return True if input is a complex type or array. |
| `isdtype` | `we.isdtype` | `np.isdtype` | free | $0$ | 🟢 supported | Return True if array or dtype is of specified kind (NumPy 2.x). |
| `isfinite` | `we.isfinite` | `np.isfinite` | counted_custom | varies | 🟠 supported | Test for finite values element-wise. Cost: numel(input). |
| `isfortran` | `we.isfortran` | `np.isfortran` | free | $0$ | 🟢 supported | Return True if array is Fortran contiguous. |
| `isin` | `we.isin` | `np.isin` | counted_custom | varies | 🟠 supported | Set membership; cost = (n+m)*ceil(log2(n+m)). |
| `isinf` | `we.isinf` | `np.isinf` | counted_custom | varies | 🟠 supported | Test for infinity element-wise. Cost: numel(input). |
| `isnan` | `we.isnan` | `np.isnan` | counted_custom | varies | 🟠 supported | Test for NaN element-wise. Cost: numel(input). |
| `isnat` | — | `np.isnat` | blacklisted | N/A | 🔴 blocked | Blacklisted per reviewer — datetime ops not in scope. |
| `isneginf` | `we.isneginf` | `np.isneginf` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Test for negative infinity element-wise. |
| `isposinf` | `we.isposinf` | `np.isposinf` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Test for positive infinity element-wise. |
| `isreal` | `we.isreal` | `np.isreal` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Test if element is real (imag == 0) element-wise. |
| `isrealobj` | `we.isrealobj` | `np.isrealobj` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Return True if x is a not complex type or array. |
| `isscalar` | `we.isscalar` | `np.isscalar` | free | $0$ | 🟢 supported | Return True if input is a scalar. |
| `issubdtype` | `we.issubdtype` | `np.issubdtype` | free | $0$ | 🟢 supported | Return True if first argument is lower in type hierarchy. |
| `iterable` | `we.iterable` | `np.iterable` | free | $0$ | 🟢 supported | Return True if object is iterable. |
| `ix_` | `we.ix_` | `np.ix_` | counted_custom | varies | 🟠 supported | Construct open mesh from multiple sequences. Cost: numel(output). |
| `kaiser` | `we.kaiser` | `np.kaiser` | counted_custom | $3n$ | 🟠 supported | Kaiser window. Cost: 3*n (Bessel function eval per sample). |
| `kron` | `we.kron` | `np.kron` | counted_custom | $m_1 m_2 \cdot n_1 n_2$ | 🟠 supported | Kronecker product; cost proportional to output size. |
| `lcm` | `we.lcm` | `np.lcm` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise least common multiple. |
| `ldexp` | `we.ldexp` | `np.ldexp` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Return x1 * 2**x2 element-wise. |
| `left_shift` | `we.left_shift` | `np.left_shift` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise left bit shift (legacy name). |
| `less` | `we.less` | `np.less` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise x1 &lt; x2. |
| `less_equal` | `we.less_equal` | `np.less_equal` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise x1 &le; x2. |
| `lexsort` | `we.lexsort` | `np.lexsort` | counted_custom | varies | 🟠 supported | Multi-key sort; cost = k*n*ceil(log2(n)). |
| `linalg.cholesky` | `we.linalg.cholesky` | `np.linalg.cholesky` | counted_custom | $n^3$ | 🟠 supported | Cholesky decomposition. Cost: $n^3$. |
| `linalg.cond` | `we.linalg.cond` | `np.linalg.cond` | counted_custom | $m \cdot n \cdot \min(m,n)$ | 🟠 supported | Condition number. Cost: m*n*min(m,n) (via SVD). |
| `linalg.cross` | `we.linalg.cross` | `np.linalg.cross` | counted_custom | delegates to `cross` | 🟠 supported | Delegates to `we.cross` which charges `numel(output)` FLOPs. |
| `linalg.det` | `we.linalg.det` | `np.linalg.det` | counted_custom | $n^3$ | 🟠 supported | Determinant. Cost: $n^3$. |
| `linalg.diagonal` | `we.linalg.diagonal` | `np.linalg.diagonal` | free | $0$ | 🟢 supported | View of diagonal — delegates to whest.diagonal. Cost: 0 FLOPs. |
| `linalg.eig` | `we.linalg.eig` | `np.linalg.eig` | counted_custom | $n^3$ | 🟠 supported | Eigendecomposition. Cost: $n^3$. |
| `linalg.eigh` | `we.linalg.eigh` | `np.linalg.eigh` | counted_custom | $n^3$ | 🟠 supported | Symmetric eigendecomposition. Cost: $n^3$. |
| `linalg.eigvals` | `we.linalg.eigvals` | `np.linalg.eigvals` | counted_custom | $n^3$ | 🟠 supported | Eigenvalues only. Cost: $n^3$. |
| `linalg.eigvalsh` | `we.linalg.eigvalsh` | `np.linalg.eigvalsh` | counted_custom | $n^3$ | 🟠 supported | Symmetric eigenvalues. Cost: $n^3$. |
| `linalg.inv` | `we.linalg.inv` | `np.linalg.inv` | counted_custom | $n^3$ | 🟠 supported | Matrix inverse. Cost: $n^3$ (LU + solve). |
| `linalg.lstsq` | `we.linalg.lstsq` | `np.linalg.lstsq` | counted_custom | $m \cdot n \cdot \min(m,n)$ | 🟠 supported | Least squares. Cost: m*n*min(m,n) (LAPACK gelsd/SVD). |
| `linalg.matmul` | `we.linalg.matmul` | `np.linalg.matmul` | counted_custom | delegates to `matmul` | 🟠 supported | Delegates to `we.matmul` which charges `m*k*n` FLOPs (FMA=1). |
| `linalg.matrix_norm` | `we.linalg.matrix_norm` | `np.linalg.matrix_norm` | counted_custom | varies | 🟠 supported | Matrix norm. Cost depends on ord: 2*numel for Frobenius, m*n*min(m,n) for ord=2. |
| `linalg.matrix_power` | `we.linalg.matrix_power` | `np.linalg.matrix_power` | counted_custom | $\lfloor\log_2 k\rfloor \cdot n^3$ | 🟠 supported | Matrix power. Cost: $(\lfloor\log_2 k\rfloor + \text\{popcount\}(k) - 1) \cdot n^3$ (exponentiation by squaring). |
| `linalg.matrix_rank` | `we.linalg.matrix_rank` | `np.linalg.matrix_rank` | counted_custom | $m \cdot n \cdot \min(m,n)$ | 🟠 supported | Matrix rank. Cost: m*n*min(m,n) (via SVD). |
| `linalg.matrix_transpose` | `we.linalg.matrix_transpose` | `np.linalg.matrix_transpose` | free | $0$ | 🟢 supported | Transpose view — delegates to whest.matrix_transpose. Cost: 0 FLOPs. |
| `linalg.multi_dot` | `we.linalg.multi_dot` | `np.linalg.multi_dot` | counted_custom | optimal chain | 🟠 supported | Chain matmul. Cost: sum of optimal chain matmul costs (CLRS §15.2). |
| `linalg.norm` | `we.linalg.norm` | `np.linalg.norm` | counted_custom | varies | 🟠 supported | Norm. Cost depends on ord: numel for L1/inf, 2*numel for Frobenius, m*n*min(m,n) for ord=2. |
| `linalg.outer` | `we.linalg.outer` | `np.linalg.outer` | counted_custom | delegates to `outer` | 🟠 supported | Delegates to `we.outer` which charges `m*n` FLOPs. |
| `linalg.pinv` | `we.linalg.pinv` | `np.linalg.pinv` | counted_custom | $m \cdot n \cdot \min(m,n)$ | 🟠 supported | Pseudoinverse. Cost: m*n*min(m,n) (via SVD). |
| `linalg.qr` | `we.linalg.qr` | `np.linalg.qr` | counted_custom | $m \cdot n \cdot \min(m,n)$ | 🟠 supported | QR decomposition. Cost: $m \cdot n \cdot \min(m,n)$. |
| `linalg.slogdet` | `we.linalg.slogdet` | `np.linalg.slogdet` | counted_custom | $n^3$ | 🟠 supported | Sign + log determinant. Cost: $n^3$. |
| `linalg.solve` | `we.linalg.solve` | `np.linalg.solve` | counted_custom | $n^3$ | 🟠 supported | Solve Ax=b. Cost: $n^3$. |
| `linalg.svd` | `we.linalg.svd` | `np.linalg.svd` | counted_custom | $m \cdot n \cdot k$ | 🟠 supported | Singular value decomposition; cost ~ O(min(m,n)*m*n). |
| `linalg.svdvals` | `we.linalg.svdvals` | `np.linalg.svdvals` | counted_custom | $m \cdot n \cdot \min(m,n)$ | 🟠 supported | Singular values only. Cost: m*n*min(m,n) (Golub-Reinsch). |
| `linalg.tensordot` | `we.linalg.tensordot` | `np.linalg.tensordot` | counted_custom | delegates to `tensordot` | 🟠 supported | Delegates to `we.tensordot` which charges FLOPs based on contraction. |
| `linalg.tensorinv` | `we.linalg.tensorinv` | `np.linalg.tensorinv` | counted_custom | $n^3$ | 🟠 supported | Tensor inverse. Cost: $n^3$ after reshape (delegates to inv). |
| `linalg.tensorsolve` | `we.linalg.tensorsolve` | `np.linalg.tensorsolve` | counted_custom | $n^3$ | 🟠 supported | Tensor solve. Cost: $n^3$ after reshape (delegates to solve). |
| `linalg.trace` | `we.linalg.trace` | `np.linalg.trace` | counted_custom | $n$ | 🟠 supported | Matrix trace. Cost: n (sum of diagonal elements). |
| `linalg.vecdot` | `we.linalg.vecdot` | `np.linalg.vecdot` | counted_custom | delegates to `vecdot` | 🟠 supported | Delegates to `we.vecdot` which charges `2*n` FLOPs. |
| `linalg.vector_norm` | `we.linalg.vector_norm` | `np.linalg.vector_norm` | counted_custom | $n$ | 🟠 supported | Vector norm. Cost: numel (or 2*numel for general p-norm). |
| `linspace` | `we.linspace` | `np.linspace` | counted_custom | varies | 🟠 supported | Return evenly spaced numbers over interval. Cost: numel(output). |
| `load` | — | `np.load` | blacklisted | N/A | 🔴 blocked | Load arrays from .npy/.npz files. Not supported. |
| `loadtxt` | — | `np.loadtxt` | blacklisted | N/A | 🔴 blocked | Load data from text file. Not supported. |
| `log` | `we.log` | `np.log` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise natural logarithm. |
| `log10` | `we.log10` | `np.log10` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise base-10 logarithm. |
| `log1p` | `we.log1p` | `np.log1p` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise log(1+x) (accurate near zero). |
| `log2` | `we.log2` | `np.log2` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise base-2 logarithm. |
| `logaddexp` | `we.logaddexp` | `np.logaddexp` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | log(exp(x1) + exp(x2)) element-wise. |
| `logaddexp2` | `we.logaddexp2` | `np.logaddexp2` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | log2(2**x1 + 2**x2) element-wise. |
| `logical_and` | `we.logical_and` | `np.logical_and` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise logical AND. |
| `logical_not` | `we.logical_not` | `np.logical_not` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise logical NOT. |
| `logical_or` | `we.logical_or` | `np.logical_or` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise logical OR. |
| `logical_xor` | `we.logical_xor` | `np.logical_xor` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise logical XOR. |
| `logspace` | `we.logspace` | `np.logspace` | counted_custom | varies | 🟠 supported | Log-spaced generation; cost = num. |
| `mask_indices` | `we.mask_indices` | `np.mask_indices` | counted_custom | varies | 🟠 supported | Return indices of mask for n x n array. Cost: numel(output). |
| `matmul` | `we.matmul` | `np.matmul` | counted_custom | $m \cdot k \cdot n$ | 🟠 supported | Matrix multiplication; cost = M*K*N (FMA=1). |
| `matrix_transpose` | `we.matrix_transpose` | `np.matrix_transpose` | free | $0$ | 🟢 supported | Transpose last two dimensions (NumPy 2.x array API). |
| `matvec` | `we.matvec` | `np.matvec` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Matrix-vector product. Cost = output_size * contracted_axis. |
| `max` | `we.max` | `np.max` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Maximum value of array. |
| `maximum` | `we.maximum` | `np.maximum` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise maximum (propagates NaN). |
| `may_share_memory` | `we.may_share_memory` | `np.may_share_memory` | free | $0$ | 🟢 supported | Determine if two arrays might share memory. |
| `mean` | `we.mean` | `np.mean` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Arithmetic mean of array elements. |
| `median` | `we.median` | `np.median` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Median of array elements (sorts internally). |
| `meshgrid` | `we.meshgrid` | `np.meshgrid` | counted_custom | varies | 🟠 supported | Coordinate matrices from coordinate vectors. Cost: numel(output). |
| `min` | `we.min` | `np.min` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Minimum value of array. |
| `min_scalar_type` | `we.min_scalar_type` | `np.min_scalar_type` | free | $0$ | 🟢 supported | Return the minimum scalar type for a value. |
| `minimum` | `we.minimum` | `np.minimum` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise minimum (propagates NaN). |
| `mintypecode` | `we.mintypecode` | `np.mintypecode` | free | $0$ | 🟢 supported | Return minimum data type character that can satisfy all given types. |
| `mod` | `we.mod` | `np.mod` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise modulo. |
| `modf` | `we.modf` | `np.modf` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Return fractional and integral parts element-wise. |
| `moveaxis` | `we.moveaxis` | `np.moveaxis` | free | $0$ | 🟢 supported | Move axes to new positions. |
| `multiply` | `we.multiply` | `np.multiply` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise multiplication. |
| `nan_to_num` | `we.nan_to_num` | `np.nan_to_num` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Replace NaN/inf with finite numbers element-wise. |
| `nanargmax` | `we.nanargmax` | `np.nanargmax` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Index of maximum ignoring NaNs. |
| `nanargmin` | `we.nanargmin` | `np.nanargmin` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Index of minimum ignoring NaNs. |
| `nancumprod` | `we.nancumprod` | `np.nancumprod` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Cumulative product ignoring NaNs. |
| `nancumsum` | `we.nancumsum` | `np.nancumsum` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Cumulative sum ignoring NaNs. |
| `nanmax` | `we.nanmax` | `np.nanmax` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Maximum ignoring NaNs. |
| `nanmean` | `we.nanmean` | `np.nanmean` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Mean ignoring NaNs. |
| `nanmedian` | `we.nanmedian` | `np.nanmedian` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Median ignoring NaNs. |
| `nanmin` | `we.nanmin` | `np.nanmin` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Minimum ignoring NaNs. |
| `nanpercentile` | `we.nanpercentile` | `np.nanpercentile` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | q-th percentile ignoring NaNs. |
| `nanprod` | `we.nanprod` | `np.nanprod` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Product ignoring NaNs. |
| `nanquantile` | `we.nanquantile` | `np.nanquantile` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | q-th quantile ignoring NaNs. |
| `nanstd` | `we.nanstd` | `np.nanstd` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Standard deviation ignoring NaNs. |
| `nansum` | `we.nansum` | `np.nansum` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Sum ignoring NaNs. |
| `nanvar` | `we.nanvar` | `np.nanvar` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Variance ignoring NaNs. |
| `ndim` | `we.ndim` | `np.ndim` | free | $0$ | 🟢 supported | Return number of dimensions of array. |
| `negative` | `we.negative` | `np.negative` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise negation. |
| `nested_iters` | — | `np.nested_iters` | blacklisted | N/A | 🔴 blocked | Create nested iterators for multi-index broadcasting. Not supported. |
| `nextafter` | `we.nextafter` | `np.nextafter` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Return next float after x1 toward x2 element-wise. |
| `nonzero` | `we.nonzero` | `np.nonzero` | counted_custom | varies | 🟠 supported | Return indices of non-zero elements. Cost: numel(input). |
| `not_equal` | `we.not_equal` | `np.not_equal` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise x1 != x2. |
| `ones` | `we.ones` | `np.ones` | free | $0$ | 🟢 supported | Create one-filled array. |
| `ones_like` | `we.ones_like` | `np.ones_like` | free | $0$ | 🟢 supported | Array of ones with same shape/type as input. |
| `outer` | `we.outer` | `np.outer` | counted_custom | $m \cdot n$ | 🟠 supported | Outer product of two vectors; cost = M*N. |
| `packbits` | `we.packbits` | `np.packbits` | counted_custom | varies | 🟠 supported | Pack elements of array into bits. Cost: numel(input). |
| `pad` | `we.pad` | `np.pad` | counted_custom | varies | 🟠 supported | Pad array. Cost: numel(output). |
| `partition` | `we.partition` | `np.partition` | counted_custom | varies | 🟠 supported | Quickselect; cost = n per slice. |
| `percentile` | `we.percentile` | `np.percentile` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | q-th percentile of array elements. |
| `permute_dims` | `we.permute_dims` | `np.permute_dims` | free | $0$ | 🟢 supported | Permute dimensions (NumPy 2.x array API). |
| `piecewise` | `we.piecewise` | `np.piecewise` | counted_custom | varies | 🟠 supported | Piecewise function. Cost: numel(input). |
| `place` | `we.place` | `np.place` | counted_custom | varies | 🟠 supported | Change elements satisfying condition. Cost: numel(input). |
| `poly` | `we.poly` | `np.poly` | counted_custom | $n^2$ | 🟠 supported | Polynomial from roots. Cost: $n^2$ FLOPs. |
| `polyadd` | `we.polyadd` | `np.polyadd` | counted_custom | $\max(n_1, n_2)$ | 🟠 supported | Add two polynomials. Cost: max(n1, n2) FLOPs. |
| `polyder` | `we.polyder` | `np.polyder` | counted_custom | $n$ | 🟠 supported | Differentiate polynomial. Cost: n FLOPs. |
| `polydiv` | `we.polydiv` | `np.polydiv` | counted_custom | $n_1 \cdot n_2$ | 🟠 supported | Divide one polynomial by another. Cost: n1 * n2 FLOPs. |
| `polyfit` | `we.polyfit` | `np.polyfit` | counted_custom | $2m \cdot (\text\{deg\}+1)^2$ | 🟠 supported | Least squares polynomial fit. Cost: 2 * m * (deg+1)^2 FLOPs. |
| `polyint` | `we.polyint` | `np.polyint` | counted_custom | $n$ | 🟠 supported | Integrate polynomial. Cost: n FLOPs. |
| `polymul` | `we.polymul` | `np.polymul` | counted_custom | $n_1 \cdot n_2$ | 🟠 supported | Multiply polynomials. Cost: n1 * n2 FLOPs. |
| `polysub` | `we.polysub` | `np.polysub` | counted_custom | $\max(n_1, n_2)$ | 🟠 supported | Difference (subtraction) of two polynomials. Cost: max(n1, n2) FLOPs. |
| `polyval` | `we.polyval` | `np.polyval` | counted_custom | $m \cdot \text\{deg\}$ | 🟠 supported | Evaluate polynomial at given points. Cost: $m \cdot \text\{deg\}$ (Horner's method, FMA=1). |
| `positive` | `we.positive` | `np.positive` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise unary plus (copy with sign preserved). |
| `pow` | `we.pow` | `np.pow` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Alias for power (NumPy 2.x). |
| `power` | `we.power` | `np.power` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise exponentiation x**y. |
| `prod` | `we.prod` | `np.prod` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Product of array elements. |
| `promote_types` | `we.promote_types` | `np.promote_types` | free | $0$ | 🟢 supported | Return smallest type to which both types may be safely cast. |
| `ptp` | `we.ptp` | `np.ptp` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Peak-to-peak (max - min) range of array. |
| `put` | `we.put` | `np.put` | counted_custom | varies | 🟠 supported | Replace elements at given flat indices. Cost: numel(input). |
| `put_along_axis` | `we.put_along_axis` | `np.put_along_axis` | counted_custom | varies | 🟠 supported | Put values into destination array using indices. Cost: numel(input). |
| `putmask` | `we.putmask` | `np.putmask` | counted_custom | varies | 🟠 supported | Change elements of array based on condition and input values. Cost: numel(input). |
| `quantile` | `we.quantile` | `np.quantile` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | q-th quantile of array elements. |
| `rad2deg` | `we.rad2deg` | `np.rad2deg` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Alias for degrees. |
| `radians` | `we.radians` | `np.radians` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Convert degrees to radians element-wise. |
| `random.beta` | `we.random.beta` | `np.random.beta` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.binomial` | `we.random.binomial` | `np.random.binomial` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.bytes` | `we.random.bytes` | `np.random.bytes` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.chisquare` | `we.random.chisquare` | `np.random.chisquare` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.choice` | `we.random.choice` | `np.random.choice` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output) if replace, n*ceil(log2(n)) if not. |
| `random.default_rng` | `we.random.default_rng` | `np.random.default_rng` | free | $0$ | 🟢 supported | Construct a new Generator with default BitGenerator. |
| `random.dirichlet` | `we.random.dirichlet` | `np.random.dirichlet` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.exponential` | `we.random.exponential` | `np.random.exponential` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.f` | `we.random.f` | `np.random.f` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.gamma` | `we.random.gamma` | `np.random.gamma` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.geometric` | `we.random.geometric` | `np.random.geometric` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.get_state` | `we.random.get_state` | `np.random.get_state` | free | $0$ | 🟢 supported | Return tuple representing internal state of generator. |
| `random.gumbel` | `we.random.gumbel` | `np.random.gumbel` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.hypergeometric` | `we.random.hypergeometric` | `np.random.hypergeometric` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.laplace` | `we.random.laplace` | `np.random.laplace` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.logistic` | `we.random.logistic` | `np.random.logistic` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.lognormal` | `we.random.lognormal` | `np.random.lognormal` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.logseries` | `we.random.logseries` | `np.random.logseries` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.multinomial` | `we.random.multinomial` | `np.random.multinomial` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.multivariate_normal` | `we.random.multivariate_normal` | `np.random.multivariate_normal` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.negative_binomial` | `we.random.negative_binomial` | `np.random.negative_binomial` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.noncentral_chisquare` | `we.random.noncentral_chisquare` | `np.random.noncentral_chisquare` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.noncentral_f` | `we.random.noncentral_f` | `np.random.noncentral_f` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.normal` | `we.random.normal` | `np.random.normal` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.pareto` | `we.random.pareto` | `np.random.pareto` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.permutation` | `we.random.permutation` | `np.random.permutation` | counted_custom | varies | 🟠 supported | Shuffle; cost = n*ceil(log2(n)). |
| `random.poisson` | `we.random.poisson` | `np.random.poisson` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.power` | `we.random.power` | `np.random.power` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.rand` | `we.random.rand` | `np.random.rand` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.randint` | `we.random.randint` | `np.random.randint` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.randn` | `we.random.randn` | `np.random.randn` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.random` | `we.random.random` | `np.random.random` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.random_integers` | `we.random.random_integers` | `np.random.random_integers` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.random_sample` | `we.random.random_sample` | `np.random.random_sample` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.ranf` | `we.random.ranf` | `np.random.ranf` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.rayleigh` | `we.random.rayleigh` | `np.random.rayleigh` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.sample` | `we.random.sample` | `np.random.sample` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.seed` | `we.random.seed` | `np.random.seed` | free | $0$ | 🟢 supported | Seed random number generator. |
| `random.set_state` | `we.random.set_state` | `np.random.set_state` | free | $0$ | 🟢 supported | Set internal state of generator. |
| `random.shuffle` | `we.random.shuffle` | `np.random.shuffle` | counted_custom | varies | 🟠 supported | Shuffle; cost = n*ceil(log2(n)). |
| `random.standard_cauchy` | `we.random.standard_cauchy` | `np.random.standard_cauchy` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.standard_exponential` | `we.random.standard_exponential` | `np.random.standard_exponential` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.standard_gamma` | `we.random.standard_gamma` | `np.random.standard_gamma` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.standard_normal` | `we.random.standard_normal` | `np.random.standard_normal` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.standard_t` | `we.random.standard_t` | `np.random.standard_t` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.triangular` | `we.random.triangular` | `np.random.triangular` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.uniform` | `we.random.uniform` | `np.random.uniform` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.vonmises` | `we.random.vonmises` | `np.random.vonmises` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.wald` | `we.random.wald` | `np.random.wald` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.weibull` | `we.random.weibull` | `np.random.weibull` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `random.zipf` | `we.random.zipf` | `np.random.zipf` | counted_custom | varies | 🟠 supported | Sampling; cost = numel(output). |
| `ravel` | `we.ravel` | `np.ravel` | free | $0$ | 🟢 supported | Return contiguous flattened array. Cost: numel(input). |
| `ravel_multi_index` | `we.ravel_multi_index` | `np.ravel_multi_index` | free | $0$ | 🟢 supported | Convert multi-dimensional index to flat index. |
| `real` | `we.real` | `np.real` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Return real part of complex array. |
| `real_if_close` | `we.real_if_close` | `np.real_if_close` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Return real array if imaginary part is negligible. |
| `reciprocal` | `we.reciprocal` | `np.reciprocal` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise 1/x. |
| `remainder` | `we.remainder` | `np.remainder` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise remainder (same as mod). |
| `repeat` | `we.repeat` | `np.repeat` | counted_custom | varies | 🟠 supported | Repeat elements of an array. Cost: numel(output). |
| `require` | `we.require` | `np.require` | free | $0$ | 🟢 supported | Return array that satisfies requirements. |
| `reshape` | `we.reshape` | `np.reshape` | free | $0$ | 🟢 supported | Reshape array without copying. |
| `resize` | `we.resize` | `np.resize` | counted_custom | varies | 🟠 supported | Return new array with given shape by repeating. Cost: numel(output). |
| `result_type` | `we.result_type` | `np.result_type` | free | $0$ | 🟢 supported | Return type that results from applying NumPy type promotion. |
| `right_shift` | `we.right_shift` | `np.right_shift` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise right bit shift (legacy name). |
| `rint` | `we.rint` | `np.rint` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Round to nearest integer element-wise. |
| `roll` | `we.roll` | `np.roll` | counted_custom | varies | 🟠 supported | Roll array elements along axis. Cost: numel(output). |
| `rollaxis` | `we.rollaxis` | `np.rollaxis` | free | $0$ | 🟢 supported | Roll specified axis backwards. Cost: numel(output). |
| `roots` | `we.roots` | `np.roots` | counted_custom | $10n^3$ | 🟠 supported | Return roots of polynomial with given coefficients. Cost: $n^3$ (companion matrix eig, simplified). |
| `rot90` | `we.rot90` | `np.rot90` | free | $0$ | 🟢 supported | Rotate array 90 degrees. |
| `round` | `we.round` | `np.round` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Round to given number of decimals element-wise. |
| `row_stack` | `we.row_stack` | `np.row_stack` | free | $0$ | 🟢 supported | Stack arrays vertically (alias for vstack). |
| `save` | — | `np.save` | blacklisted | N/A | 🔴 blocked | Save array to .npy file. Not supported. |
| `savetxt` | — | `np.savetxt` | blacklisted | N/A | 🔴 blocked | Save array to text file. Not supported. |
| `savez` | — | `np.savez` | blacklisted | N/A | 🔴 blocked | Save multiple arrays to .npz file. Not supported. |
| `savez_compressed` | — | `np.savez_compressed` | blacklisted | N/A | 🔴 blocked | Save multiple arrays to compressed .npz file. Not supported. |
| `searchsorted` | `we.searchsorted` | `np.searchsorted` | counted_custom | varies | 🟠 supported | Binary search; cost = m*ceil(log2(n)). |
| `select` | `we.select` | `np.select` | counted_custom | varies | 🟠 supported | Return array from list of choices based on conditions. Cost: numel(input). |
| `setbufsize` | — | `np.setbufsize` | blacklisted | N/A | 🔴 blocked | Set size of buffer used in ufuncs. Not supported. |
| `setdiff1d` | `we.setdiff1d` | `np.setdiff1d` | counted_custom | varies | 🟠 supported | Set difference; cost = (n+m)*ceil(log2(n+m)). |
| `seterr` | — | `np.seterr` | blacklisted | N/A | 🔴 blocked | Set how floating-point errors are handled. Not supported. |
| `seterrcall` | — | `np.seterrcall` | blacklisted | N/A | 🔴 blocked | Set callback function for floating-point errors. Not supported. |
| `setxor1d` | `we.setxor1d` | `np.setxor1d` | counted_custom | varies | 🟠 supported | Symmetric set difference; cost = (n+m)*ceil(log2(n+m)). |
| `shape` | `we.shape` | `np.shape` | free | $0$ | 🟢 supported | Return shape of array. |
| `shares_memory` | `we.shares_memory` | `np.shares_memory` | free | $0$ | 🟢 supported | Determine if two arrays share memory. |
| `show_config` | — | `np.show_config` | blacklisted | N/A | 🔴 blocked | Show NumPy build configuration. Not supported. |
| `show_runtime` | — | `np.show_runtime` | blacklisted | N/A | 🔴 blocked | Show runtime info. Not supported. |
| `sign` | `we.sign` | `np.sign` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise sign function. |
| `signbit` | `we.signbit` | `np.signbit` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Returns True for elements with negative sign bit. |
| `sin` | `we.sin` | `np.sin` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise sine. |
| `sinc` | `we.sinc` | `np.sinc` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Normalized sinc function element-wise. |
| `sinh` | `we.sinh` | `np.sinh` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise hyperbolic sine. |
| `size` | `we.size` | `np.size` | free | $0$ | 🟢 supported | Return number of elements in array. |
| `sort` | `we.sort` | `np.sort` | counted_custom | varies | 🟠 supported | Comparison sort; cost = n*ceil(log2(n)) per slice. |
| `sort_complex` | `we.sort_complex` | `np.sort_complex` | counted_custom | varies | 🟠 supported | Sort complex array. Cost: $n \cdot \lceil\log_2 n\rceil$. |
| `spacing` | `we.spacing` | `np.spacing` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Return ULP spacing for each element. |
| `split` | `we.split` | `np.split` | free | $0$ | 🟢 supported | Split array into sub-arrays. Cost: numel(output). |
| `sqrt` | `we.sqrt` | `np.sqrt` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise square root. |
| `square` | `we.square` | `np.square` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise x^2. |
| `squeeze` | `we.squeeze` | `np.squeeze` | free | $0$ | 🟢 supported | Remove size-1 dimensions. |
| `stack` | `we.stack` | `np.stack` | counted_custom | varies | 🟠 supported | Join arrays along new axis. Cost: numel(output). |
| `stats.cauchy.cdf` | `we.stats.cauchy.cdf` | `np.stats.cauchy.cdf` | counted_custom | $5n$ | 🟠 supported | Cauchy CDF; cost = numel(input). |
| `stats.cauchy.pdf` | `we.stats.cauchy.pdf` | `np.stats.cauchy.pdf` | counted_custom | $6n$ | 🟠 supported | Cauchy PDF; cost = numel(input). |
| `stats.cauchy.ppf` | `we.stats.cauchy.ppf` | `np.stats.cauchy.ppf` | counted_custom | $5n$ | 🟠 supported | Cauchy PPF; cost = numel(input). |
| `stats.expon.cdf` | `we.stats.expon.cdf` | `np.stats.expon.cdf` | counted_custom | $5n$ | 🟠 supported | Exponential CDF; cost = numel(input). |
| `stats.expon.pdf` | `we.stats.expon.pdf` | `np.stats.expon.pdf` | counted_custom | $5n$ | 🟠 supported | Exponential PDF; cost = numel(input). |
| `stats.expon.ppf` | `we.stats.expon.ppf` | `np.stats.expon.ppf` | counted_custom | $5n$ | 🟠 supported | Exponential PPF; cost = numel(input). |
| `stats.laplace.cdf` | `we.stats.laplace.cdf` | `np.stats.laplace.cdf` | counted_custom | $5n$ | 🟠 supported | Laplace CDF; cost = numel(input). |
| `stats.laplace.pdf` | `we.stats.laplace.pdf` | `np.stats.laplace.pdf` | counted_custom | $5n$ | 🟠 supported | Laplace PDF; cost = numel(input). |
| `stats.laplace.ppf` | `we.stats.laplace.ppf` | `np.stats.laplace.ppf` | counted_custom | $5n$ | 🟠 supported | Laplace PPF; cost = numel(input). |
| `stats.logistic.cdf` | `we.stats.logistic.cdf` | `np.stats.logistic.cdf` | counted_custom | $5n$ | 🟠 supported | Logistic CDF; cost = numel(input). |
| `stats.logistic.pdf` | `we.stats.logistic.pdf` | `np.stats.logistic.pdf` | counted_custom | $8n$ | 🟠 supported | Logistic PDF; cost = numel(input). |
| `stats.logistic.ppf` | `we.stats.logistic.ppf` | `np.stats.logistic.ppf` | counted_custom | $5n$ | 🟠 supported | Logistic PPF; cost = numel(input). |
| `stats.lognorm.cdf` | `we.stats.lognorm.cdf` | `np.stats.lognorm.cdf` | counted_custom | $25n$ | 🟠 supported | Log-normal CDF; cost = numel(input). |
| `stats.lognorm.pdf` | `we.stats.lognorm.pdf` | `np.stats.lognorm.pdf` | counted_custom | $15n$ | 🟠 supported | Log-normal PDF; cost = numel(input). |
| `stats.lognorm.ppf` | `we.stats.lognorm.ppf` | `np.stats.lognorm.ppf` | counted_custom | $45n$ | 🟠 supported | Log-normal PPF; cost = numel(input). |
| `stats.norm.cdf` | `we.stats.norm.cdf` | `np.stats.norm.cdf` | counted_custom | $20n$ | 🟠 supported | Normal CDF; cost = numel(input). |
| `stats.norm.pdf` | `we.stats.norm.pdf` | `np.stats.norm.pdf` | counted_custom | $10n$ | 🟠 supported | Normal PDF; cost = numel(input). |
| `stats.norm.ppf` | `we.stats.norm.ppf` | `np.stats.norm.ppf` | counted_custom | $40n$ | 🟠 supported | Normal PPF (inverse CDF); cost = numel(input). |
| `stats.truncnorm.cdf` | `we.stats.truncnorm.cdf` | `np.stats.truncnorm.cdf` | counted_custom | $30n$ | 🟠 supported | Truncated normal CDF; cost = numel(input). |
| `stats.truncnorm.pdf` | `we.stats.truncnorm.pdf` | `np.stats.truncnorm.pdf` | counted_custom | $30n$ | 🟠 supported | Truncated normal PDF; cost = numel(input). |
| `stats.truncnorm.ppf` | `we.stats.truncnorm.ppf` | `np.stats.truncnorm.ppf` | counted_custom | $50n$ | 🟠 supported | Truncated normal PPF; cost = numel(input). |
| `stats.uniform.cdf` | `we.stats.uniform.cdf` | `np.stats.uniform.cdf` | counted_custom | $3n$ | 🟠 supported | Uniform CDF; cost = numel(input). |
| `stats.uniform.pdf` | `we.stats.uniform.pdf` | `np.stats.uniform.pdf` | counted_custom | $3n$ | 🟠 supported | Uniform PDF; cost = numel(input). |
| `stats.uniform.ppf` | `we.stats.uniform.ppf` | `np.stats.uniform.ppf` | counted_custom | $3n$ | 🟠 supported | Uniform PPF; cost = numel(input). |
| `std` | `we.std` | `np.std` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Standard deviation; cost_multiplier=2 (two passes). |
| `subtract` | `we.subtract` | `np.subtract` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise subtraction. |
| `sum` | `we.sum` | `np.sum` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Sum of array elements. |
| `swapaxes` | `we.swapaxes` | `np.swapaxes` | free | $0$ | 🟢 supported | Interchange two axes of an array. |
| `take` | `we.take` | `np.take` | counted_custom | varies | 🟠 supported | Take elements from array along axis. Cost: numel(output). |
| `take_along_axis` | `we.take_along_axis` | `np.take_along_axis` | counted_custom | varies | 🟠 supported | Take values from input array by matching 1-D index. Cost: numel(output). |
| `tan` | `we.tan` | `np.tan` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise tangent. |
| `tanh` | `we.tanh` | `np.tanh` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise hyperbolic tangent. |
| `tensordot` | `we.tensordot` | `np.tensordot` | counted_custom | $\prod_i d_i$ | 🟠 supported | Tensor dot product along specified axes. |
| `tile` | `we.tile` | `np.tile` | counted_custom | varies | 🟠 supported | Repeat array by tiling. Cost: numel(output). |
| `trace` | `we.trace` | `np.trace` | counted_custom | varies | 🟠 supported | Diagonal sum; cost = min(n,m). |
| `transpose` | `we.transpose` | `np.transpose` | free | $0$ | 🟢 supported | Permute array dimensions. |
| `trapezoid` | `we.trapezoid` | `np.trapezoid` | counted_custom | $\text\{numel\}(\text\{input\})$ | 🟠 supported | Integrate using the trapezoidal rule. |
| `trapz` | `we.trapz` | `np.trapz` | counted_custom | $\text\{numel\}(\text\{input\})$ | 🟠 supported | Alias for trapezoid (deprecated). |
| `tri` | `we.tri` | `np.tri` | free | $0$ | 🟢 supported | Array with ones at and below given diagonal. |
| `tril` | `we.tril` | `np.tril` | free | $0$ | 🟢 supported | Lower triangle of array. |
| `tril_indices` | `we.tril_indices` | `np.tril_indices` | free | $0$ | 🟢 supported | Return lower-triangle indices for n x n array. |
| `tril_indices_from` | `we.tril_indices_from` | `np.tril_indices_from` | free | $0$ | 🟢 supported | Return lower-triangle indices for given array. |
| `trim_zeros` | `we.trim_zeros` | `np.trim_zeros` | counted_custom | varies | 🟠 supported | Trim leading/trailing zeros from 1-D array. Cost: num trimmed. |
| `triu` | `we.triu` | `np.triu` | free | $0$ | 🟢 supported | Upper triangle of array. |
| `triu_indices` | `we.triu_indices` | `np.triu_indices` | free | $0$ | 🟢 supported | Return upper-triangle indices for n x n array. |
| `triu_indices_from` | `we.triu_indices_from` | `np.triu_indices_from` | free | $0$ | 🟢 supported | Return upper-triangle indices for given array. |
| `true_divide` | `we.true_divide` | `np.true_divide` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Element-wise true division (explicit). |
| `trunc` | `we.trunc` | `np.trunc` | counted_unary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Truncate toward zero element-wise. |
| `typename` | `we.typename` | `np.typename` | free | $0$ | 🟢 supported | Return description of given data type code. |
| `union1d` | `we.union1d` | `np.union1d` | counted_custom | varies | 🟠 supported | Set union; cost = (n+m)*ceil(log2(n+m)). |
| `unique` | `we.unique` | `np.unique` | counted_custom | varies | 🟠 supported | Sort-based unique; cost = n*ceil(log2(n)). |
| `unique_all` | `we.unique_all` | `np.unique_all` | counted_custom | varies | 🟠 supported | Sort-based unique; cost = n*ceil(log2(n)). |
| `unique_counts` | `we.unique_counts` | `np.unique_counts` | counted_custom | varies | 🟠 supported | Sort-based unique; cost = n*ceil(log2(n)). |
| `unique_inverse` | `we.unique_inverse` | `np.unique_inverse` | counted_custom | varies | 🟠 supported | Sort-based unique; cost = n*ceil(log2(n)). |
| `unique_values` | `we.unique_values` | `np.unique_values` | counted_custom | varies | 🟠 supported | Sort-based unique; cost = n*ceil(log2(n)). |
| `unpackbits` | `we.unpackbits` | `np.unpackbits` | counted_custom | varies | 🟠 supported | Unpack elements of array into bits. Cost: numel(input). |
| `unravel_index` | `we.unravel_index` | `np.unravel_index` | free | $0$ | 🟢 supported | Convert flat index to multi-dimensional index. |
| `unstack` | `we.unstack` | `np.unstack` | counted_custom | varies | 🟠 supported | Unstack array along axis into tuple of arrays (NumPy 2.x). Cost: numel(output). |
| `unwrap` | `we.unwrap` | `np.unwrap` | counted_custom | $\text\{numel\}(\text\{input\})$ | 🟠 supported | Phase unwrap. Cost: $\text\{numel\}(\text\{input\})$ (diff + conditional adjustment). |
| `vander` | `we.vander` | `np.vander` | counted_custom | varies | 🟠 supported | Vandermonde matrix; cost = len(x)*(N-1). |
| `var` | `we.var` | `np.var` | counted_reduction | $\text\{numel\}(\text\{input\})$ | 🟡 supported | Variance; cost_multiplier=2 (two passes). |
| `vdot` | `we.vdot` | `np.vdot` | counted_custom | $n$ | 🟠 supported | Dot product with conjugation; cost = N (FMA=1). |
| `vecdot` | `we.vecdot` | `np.vecdot` | counted_binary | $n$ | 🟡 supported | Vector dot product along last axis. |
| `vecmat` | `we.vecmat` | `np.vecmat` | counted_binary | $\text\{numel\}(\text\{output\})$ | 🟡 supported | Vector-matrix product. Cost = output_size * contracted_axis. |
| `vsplit` | `we.vsplit` | `np.vsplit` | free | $0$ | 🟢 supported | Split array into rows. Cost: numel(output). |
| `vstack` | `we.vstack` | `np.vstack` | counted_custom | varies | 🟠 supported | Stack arrays vertically. Cost: numel(output). |
| `where` | `we.where` | `np.where` | counted_custom | varies | 🟠 supported | Select elements based on condition. Cost: numel(input). |
| `zeros` | `we.zeros` | `np.zeros` | free | $0$ | 🟢 supported | Create zero-filled array. |
| `zeros_like` | `we.zeros_like` | `np.zeros_like` | free | $0$ | 🟢 supported | Array of zeros with same shape/type as input. |
