---
title: "Competition Guide"
---

*Everything you need to compete within a FLOP budget.*

**You will learn:**
- How to set budget limits with `BudgetContext`
- How to use the `@we.budget` decorator form
- How wall-time limits work via `wall_time_limit_s`
- How to read budget summaries
- Common competition pitfalls and tips

## Setting a FLOP budget

Every competition submission runs inside a FLOP budget. Use `BudgetContext` to declare how many FLOPs your code is allowed to spend:

```python
import whest as we

with we.BudgetContext(flop_budget=50_000_000, namespace="solver") as budget:
    A = we.ones((256, 256))
    x = we.ones((256,))
    h = we.einsum('ij,j->i', A, x)
    h = we.exp(h)
    result = we.sum(h)

we.budget_summary()
```

If your code exceeds the budget, whest raises `BudgetExhaustedError` *before* the offending operation executes. The error message includes the cost of the failed operation and the remaining budget.

The `namespace` parameter sets the root namespace prefix for that budget context. Nested `we.namespace(...)` scopes extend it with dotted segments, but they do not create child budgets or split the FLOP limit into separate pools.

## Decorator form

For cleaner code, use `@we.budget` to attach a budget directly to a function. The namespace defaults to the function name if omitted:

```python
import whest as we

@we.budget(flop_budget=50_000_000, namespace="forward-pass")
def forward(W, x):
    h = we.einsum('ij,j->i', W, x)
    h = we.maximum(h, 0)
    return we.sum(h)

result = forward(W, x)
we.budget_summary()
```

Each call to the decorated function draws from the same namespace budget. This is equivalent to wrapping the function body in a `BudgetContext`.

## Wall-time limits

In addition to FLOP budgets, competitions may enforce a wall-clock time limit via `wall_time_limit_s`. This prevents solutions from stalling on operations that are analytically cheap but slow in practice:

```python
with we.BudgetContext(flop_budget=10**9, wall_time_limit_s=60.0) as budget:
    # Must finish within 60 seconds AND within 1 billion FLOPs
    ...
```

If the wall-clock time is exceeded, whest raises `TimeExhaustedError`. The timer starts when the context is entered and is checked after each counted operation.

## Reading the budget summary

Call `we.budget_summary()` after your computation to see where FLOPs went; pass `by_namespace=True` when you want namespace attribution:

```text title="__rich_output__"
\x1b[36m\u256d\u2500\x1b[0m\x1b[36m\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\x1b[0m\x1b[36m \x1b[0m\x1b[1;36mwhest FLOP Budget Summary\x1b[0m\x1b[36m \x1b[0m\x1b[36m\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\x1b[0m\x1b[36m\u2500\u256e\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[1m \x1b[0m\x1b[1mBudget                   \x1b[0m\x1b[1m \x1b[0m                                      50,000,000  \x1b[36m\u2502\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[1m \x1b[0m\x1b[1mUsed                     \x1b[0m\x1b[1m \x1b[0m \x1b[32m                                 66,048  (0.1%)\x1b[0m  \x1b[36m\u2502\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[1m \x1b[0m\x1b[1mRemaining                \x1b[0m\x1b[1m \x1b[0m                             49,933,952  (99.9%)  \x1b[36m\u2502\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[32m\u256d\u2500\x1b[0m\x1b[32m\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\x1b[0m\x1b[32m \x1b[0m\x1b[1;32msolver\x1b[0m\x1b[32m \x1b[0m\x1b[32m\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\x1b[0m\x1b[32m\u2500\u256e\x1b[0m                              \x1b[36m\u2502\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[32m\u2502\x1b[0m \u250f\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2533\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2533\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2533\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2513 \x1b[32m\u2502\x1b[0m                              \x1b[36m\u2502\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[32m\u2502\x1b[0m \u2503\x1b[1m \x1b[0m\x1b[1mOperation\x1b[0m\x1b[1m \x1b[0m\u2503\x1b[1m \x1b[0m\x1b[1m     FLOPs\x1b[0m\x1b[1m \x1b[0m\u2503\x1b[1m \x1b[0m\x1b[1m    %\x1b[0m\x1b[1m \x1b[0m\u2503\x1b[1m \x1b[0m\x1b[1m Calls\x1b[0m\x1b[1m \x1b[0m\u2503 \x1b[32m\u2502\x1b[0m                              \x1b[36m\u2502\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[32m\u2502\x1b[0m \u2521\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2547\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2547\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2547\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2529 \x1b[32m\u2502\x1b[0m                              \x1b[36m\u2502\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[32m\u2502\x1b[0m \u2502\x1b[2m \x1b[0m\x1b[2meinsum   \x1b[0m\x1b[2m \x1b[0m\u2502     65,536 \u2502 99.2% \u2502\x1b[2m \x1b[0m\x1b[2m1 call\x1b[0m\x1b[2m \x1b[0m\u2502 \x1b[32m\u2502\x1b[0m                              \x1b[36m\u2502\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[32m\u2502\x1b[0m \u2502\x1b[2m \x1b[0m\x1b[2mexp      \x1b[0m\x1b[2m \x1b[0m\u2502        256 \u2502  0.4% \u2502\x1b[2m \x1b[0m\x1b[2m1 call\x1b[0m\x1b[2m \x1b[0m\u2502 \x1b[32m\u2502\x1b[0m                              \x1b[36m\u2502\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[32m\u2502\x1b[0m \u2502\x1b[2m \x1b[0m\x1b[2msum      \x1b[0m\x1b[2m \x1b[0m\u2502        256 \u2502  0.4% \u2502\x1b[2m \x1b[0m\x1b[2m1 call\x1b[0m\x1b[2m \x1b[0m\u2502 \x1b[32m\u2502\x1b[0m                              \x1b[36m\u2502\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[32m\u2502\x1b[0m \u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524 \x1b[32m\u2502\x1b[0m                              \x1b[36m\u2502\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[32m\u2502\x1b[0m \u2502\x1b[2m \x1b[0m\x1b[1;2mUsed     \x1b[0m\x1b[2m \x1b[0m\u2502 \x1b[1;32m    66,048\x1b[0m \u2502 \x1b[32m 0.1%\x1b[0m \u2502\x1b[2m \x1b[0m\x1b[2m      \x1b[0m\x1b[2m \x1b[0m\u2502 \x1b[32m\u2502\x1b[0m                              \x1b[36m\u2502\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[32m\u2502\x1b[0m \u2502\x1b[2m \x1b[0m\x1b[1;2mRemaining\x1b[0m\x1b[2m \x1b[0m\u2502 \x1b[1m49,933,952\x1b[0m \u2502       \u2502\x1b[2m \x1b[0m\x1b[2m      \x1b[0m\x1b[2m \x1b[0m\u2502 \x1b[32m\u2502\x1b[0m                              \x1b[36m\u2502\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[32m\u2502\x1b[0m \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518 \x1b[32m\u2502\x1b[0m                              \x1b[36m\u2502\x1b[0m
\x1b[36m\u2502\x1b[0m \x1b[32m\u2570\u2500\x1b[0m\x1b[32m\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\x1b[0m\x1b[32m \x1b[0m\x1b[2;32mBudget: 50,000,000\x1b[0m\x1b[32m \x1b[0m\x1b[32m\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\x1b[0m\x1b[32m\u2500\u256f\x1b[0m                              \x1b[36m\u2502\x1b[0m
\x1b[36m\u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256f\x1b[0m
```

Key things to look for:

- **Budget / Used / Remaining:** the top rows show the explicit competition budget, current spend, and remaining headroom
- **By namespace:** use `we.budget_summary(by_namespace=True)` or `budget.summary(by_namespace=True)` when you want a namespace breakdown; `solver` is the root prefix and nested scopes show up as dotted paths like `solver.precompute`
- **By operation:** this toy pass is dominated by the single `einsum`; `exp` and `sum` are tiny by comparison
- **Flat default:** the default summary stays flat unless you opt into `by_namespace=True`

For programmatic access, use `we.budget_summary_dict()`:

```python
data = we.budget_summary_dict()
print(f"Used: {data['flops_used']:,} / {data['flop_budget']:,}")

# Per-namespace breakdown:
data = we.budget_summary_dict(by_namespace=True)
print(data["by_namespace"]["solver"]["flops_used"])
print(data["by_namespace"]["solver.precompute"]["flops_used"])
```

## Quick tips for competition

**Check costs before committing budget.** Use cost query functions to estimate before executing:

```python
cost = we.flops.einsum_cost('ij,jk->ik', shapes=[(256, 256), (256, 256)])
print(f"This matmul will cost {cost:,} FLOPs")  # 16,777,216
```

**Use namespaces for phases.** Split your solution into named phases (e.g., `"init"`, `"solve"`, `"refine"`) so the budget summary shows exactly where FLOPs are spent.

**Exploit symmetry for savings.** If your tensors are symmetric, wrapping them with `we.as_symmetric()` can halve pointwise costs and significantly reduce einsum costs. See [Symmetry Savings](/docs/guides/symmetry) for details.

**Prefer cheaper operations.** A matrix-vector product via `we.einsum('ij,j->i', A, x)` costs `m*n` FLOPs, while a full matrix-matrix multiply costs `m*n*k`. Avoid computing more than you need.

**Watch out for hidden costs.** Operations like `we.array()`, `we.concatenate()`, and `we.where()` are *not* free -- each charges `numel(output)` FLOPs. Check the cost of any operation you are unsure about.

## When things go wrong

If you hit `BudgetExhaustedError`, see [Budget Planning & Debugging](/docs/guides/budget-planning) for a systematic approach to diagnosing overruns and reducing costs.
